name: Update Stats

on:
  schedule:
    - cron: '50 8 * * *'   # runs daily at midnight UTC
  workflow_dispatch:       # allow manual trigger

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Fetch traffic stats
        run: |
          mkdir -p data
          echo "Fetching stats..."
          views=$(curl -s -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
            https://api.github.com/repos/qaswarh/4any1/traffic/views)
          clones=$(curl -s -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
            https://api.github.com/repos/qaswarh/4any1/traffic/clones)

          n=$(echo $views | jq '.count')
          m=$(echo $clones | jq '.count')

          echo "{\"views\": $n, \"clones\": $m}" > data/stats.json

      - name: Commit stats.json
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add data/stats.json
          git commit -m "Update stats.json"
          git push
