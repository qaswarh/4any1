name: Update Stats

on:
  schedule:
    - cron: '0 0 * * *'   # runs daily at 08:00 UTC
  workflow_dispatch:       # allow manual trigger

permissions:
  contents: write          # allow pushing changes back to repo

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Fetch traffic stats and write stats.json
        run: |
          mkdir -p data
          views=$(curl -s -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
            https://api.github.com/repos/qaswarh/4any1/traffic/views)
          clones=$(curl -s -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
            https://api.github.com/repos/qaswarh/4any1/traffic/clones)

          echo "Views JSON: $views"
          echo "Clones JSON: $clones"
          //node[0] -e "inline js code"[1] "$views"[2] "$clones"[3]
          node -e " 
            const fs = require('fs'); //built‑in filesystem module to write files
            const views = JSON.parse(process.argv[1] || '{}');  //index shifted by 1 due to inline code
            const clones = JSON.parse(process.argv[2] || '{}'); //index shifted by 1 due to inline code
            const stats = { //new stats object
              views: views.count || 0,
              clones: clones.count || 0
            };
            //JSON.stringify(stats, null, 2) converts the object into nicely formatted JSON with 2‑space indentation
            fs.writeFileSync('data/stats.json', JSON.stringify(stats, null, 2));
          " "$views" "$clones"

      - name: Commit stats.json
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add data/stats.json
          git commit -m "Update stats.json" || echo "No changes to commit"
          git remote set-url origin https://x-access-token:${{ secrets.PAT_TOKEN }}@github.com/qaswarh/4any1.git
          git push
